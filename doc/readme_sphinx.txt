Make sure Sphinx 5.3.0 and sphinx_rtd_theme and sphinx_theme is installed.
VibTools itself must also be installed for the autodocumentation to work.

To renew the sphinx documentation do the following:

`LocVib/doc/$ sphinx-build . _build`

To call the HTML:

`LocVib/doc/_build/$ open index.html`
